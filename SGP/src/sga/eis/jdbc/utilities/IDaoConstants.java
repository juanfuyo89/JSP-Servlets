package sga.eis.jdbc.utilities;

/**
 * Interface que define las constantes que se utilizan en la aplicación para el
 * manejo de las clases Dao
 * 
 * @author Juan Carlos Fuyo González
 */
public interface IDaoConstants {
    
    /* Constantes de Numeros enteros */
    
    /** Numero 0 en formato Int. */
    int    INT_ZERO                = 0;
    /** Numero 1 en formato Int. */
    int    INT_ONE                 = 1;
    /** Numero 2 en formato Int. */
    int    INT_TWO                 = 2;
    /** Numero 3 en formato Int. */
    int    INT_THREE               = 3;
    /** Numero 4 en formato Int. */
    int    INT_FOUR                = 4;
    /** Numero 5 en formato Int. */
    int    INT_FIVE                = 5;
    /** Numero 6 en formato Int. */
    int    INT_SIX                 = 6;
    /** Numero 7 en formato Int. */
    int    INT_SEVEN               = 7;
    /** Numero 8 en formato Int. */
    int    INT_EIGTH               = 8;
    /** Numero 9 en formato Int. */
    int    INT_NINE                = 9;
    /** Numero 10 en formato Int. */
    int    INT_TEN                 = 10;
    /** Numero 11 en formato Int. */
    int    INT_ELEVEN              = 11;
    /** Numero 12 en formato Int. */
    int    INT_TWELVE              = 12;
    /** Numero 13 en formato Int. */
    int    INT_THIRTEEN            = 13;
    /** Numero 14 en formato Int. */
    int    INT_FORTEEN             = 14;
    /** Numero 15 en formato Int. */
    int    INT_FIFTEEN             = 15;
    /** Numero 16 en formato Int. */
    int    INT_SIXTEEN             = 16;
    /** Numero 17 en formato Int. */
    int    INT_SEVENTEEN           = 17;
    /** Numero 18 en formato Int. */
    int    INT_EIGHTEEN            = 18;
    /** Numero 19 en formato Int. */
    int    INT_NINETEEN            = 19;
    /** Numero 20 en formato Int. */
    int    INT_TWENTY              = 20;
    
    
    /* Constantes de Numeros enteros en formato String */
    
    /** Numero 0 en formato String. */
    String    STRING_ZERO          = "0";
    /** Numero 1 en formato String. */
    String    STRING_ONE           = "1";
    
    
    /* Nombres de las tablas de la BD e */
    
    /** Nombre de la tabla Usuarios en la BD. */
    String    TABLE_USUARIOS       = "Usuarios";
    /** Nombre de la tabla Personas en la BD. */
    String    TABLE_PERSONAS       = "Personas";
    /** Nombre de la tabla Roles en la BD. */
    String    TABLE_ROLES          = "Roles";
    /** Nombre de la tabla Tipos en la BD. */
    String    TABLE_TIPOS          = "Tipos";
    /** Nombre de la tabla Marcas en la BD. */
    String    TABLE_MARCAS         = "Marcas";
    /** Nombre de la tabla Descuentos en la BD. */
    String    TABLE_DESCUENTOS     = "Descuentos";
    /** Nombre de la tabla Puntos_venta en la BD. */
    String    TABLE_PUNTOS_VENTA   = "Puntos_venta";
    /** Nombre de la tabla Productos en la BD. */
    String    TABLE_PRODUCTOS      = "Productos";
    /** Nombre de la tabla Ventas en la BD. */
    String    TABLE_VENTAS         = "Ventas";
    /** Nombre de la tabla Productos_punto_venta en la BD. */
    String    TABLE_PROD_PTOS_VTA  = "Productos_punto_venta";
}
